#include <iostream>
#include<cmath>
using namespace std;

int main()
{
   int a,b,c;
   cin>>a>>b>>c;
   
   int d = (pow(b,2)) - (4*a*c);
   
   int r,s;
   
   
   if(d>=0)
   {
   
     r = (-b +  (sqrt(d)))/(2*a);
   
     s = (-b - (sqrt(d)))/(2*a);
     
     cout<<r<<" , "<<s<<endl;
   }
   else
   {
       cout<<"No real roots"<<endl;
   }

    return 0;
}
